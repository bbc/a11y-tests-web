{
  "name": "iplayer-web-a11y-tests",
  "version": "1.0.0",
  "description": "Uses bbc-a11y to run automated tests around accessibility on the iPlayer website",
  "main": "index.js",
  "scripts": {
    "build": "node gen-a11y.js",
    "start": "npm run build && bbc-a11y",
    "start:interactive": "npm run build && bbc-a11y --interactive",
    "start:junit": "npm run build && bbc-a11y --reporter ./junit-reporter.js",
    "start:ci": "npm run build && docker run --rm --tty --volume $PWD:/ws wildlyinaccurate/bbc-a11y-docker --config /ws/a11y.js --reporter /ws/junit-reporter.js"
  },
  "author": "Andy Smith",
  "license": "BBC",
  "dependencies": {
    "junit-report-builder": "^1.1.1"
  },
  "devDependencies": {
    "bbc-a11y": "^1.3.4"
  }
}
